# Функционал страницы профиля

## Реализованные возможности

### 1. 🖼️ Загрузка и изменение аватара

- **Компонент:** `AvatarUpload.jsx`
- **Функции:**
  - Загрузка изображений (jpg, png, gif, webp)
  - Автоматическая оптимизация через Sharp (400x400px)
  - Локальное хранение в `/public/uploads/avatars/`
  - Валидация размера (макс 5MB) и типа файла
  - Прогресс-индикатор загрузки
  - Toast-уведомления
- **Требования:** Папка `/public/uploads/avatars/` должна существовать

### 2. ✏️ Редактирование профиля

- **Компонент:** `EditProfileModal.jsx` + `ProfileInfoSection.jsx`
- **Серверный экшен:** `src/app/actions/updateUserProfile.js`
- **Редактируемые поля:**
  - ✅ Имя
  - ✅ Фамилия
  - ✅ Телефон
  - ✅ Telegram
  - ✅ WhatsApp
  - ❌ Email (только через администратора)
- **Валидация:**
  - Обязательные поля: имя, фамилия, телефон
  - Формат телефона: +79001234567 или 89001234567
  - Валидация на клиенте и сервере
- **UX:**
  - Модальное окно с удобной формой
  - Подсветка ошибок в реальном времени
  - Спиннер при сохранении
  - Toast-уведомления об успехе/ошибке
  - Автоматическое обновление страницы после сохранения

### 3. 🔐 Изменение пароля

- **Компонент:** `ChangePasswordModal.jsx` + `SecuritySection.jsx`
- **Серверный экшен:** `src/app/actions/updatePassword.js`
- **Функции:**
  - Проверка текущего пароля
  - Установка нового пароля (минимум 6 символов)
  - Подтверждение нового пароля
  - Переключение видимости пароля (иконка глаза)
  - Проверка, что новый пароль отличается от старого
- **Безопасность:**
  - Хэширование паролей через bcrypt
  - Проверка текущего пароля перед изменением
  - Валидация на клиенте и сервере
- **UX:**
  - Модальное окно с тремя полями
  - Кнопки показа/скрытия пароля
  - Подсказки и предупреждения
  - Toast-уведомления

## Структура файлов

```
src/
├── app/
│   ├── actions/
│   │   ├── getUserProfile.js         # Получение данных пользователя
│   │   ├── updateUserProfile.js      # Обновление профиля
│   │   ├── updateAvatar.js           # Обновление аватара в БД
│   │   └── updatePassword.js         # Изменение пароля
│   ├── api/
│   │   ├── upload-avatar/
│   │   │   └── route.js              # API для загрузки аватаров
│   │   └── upload-logo/
│   │       └── route.js              # API для загрузки логотипов
│   └── profile/
│       ├── page.jsx                  # Главная страница профиля
│       └── profile.css               # Стили профиля
└── components/
    ├── AvatarUpload.jsx              # Загрузка аватара
    ├── ProfileInfoSection.jsx        # Секция с основной информацией
    ├── EditProfileModal.jsx          # Модальное окно редактирования
    ├── SecuritySection.jsx           # Секция безопасности
    └── ChangePasswordModal.jsx       # Модальное окно смены пароля
```

## Архитектура

### Серверные компоненты (Server Components)

- `src/app/profile/page.jsx` - основная страница профиля
  - Получает данные пользователя через `getUserProfile()`
  - Рендерит статичные элементы и клиентские компоненты
  - SSR для лучшей производительности

### Клиентские компоненты (Client Components)

- `AvatarUpload.jsx` - интерактивная загрузка файлов
- `ProfileInfoSection.jsx` - кнопка редактирования и модалка
- `EditProfileModal.jsx` - форма редактирования
- `SecuritySection.jsx` - управление безопасностью
- `ChangePasswordModal.jsx` - форма смены пароля

### Server Actions

- Безопасное выполнение операций на сервере
- Проверка авторизации через `getSession()`
- Валидация данных
- Работа с БД через Drizzle ORM
- Автоматический revalidate кэша

## Безопасность

### ✅ Реализовано:

1. **Авторизация:**

   - Проверка JWT токена в каждом серверном экшене
   - Редирект на `/login` при отсутствии сессии

2. **Валидация:**

   - Двойная валидация (клиент + сервер)
   - Проверка форматов данных (телефон, email)
   - Минимальная длина пароля

3. **Защита паролей:**

   - Хэширование через bcrypt (10 раундов)
   - Проверка старого пароля перед изменением
   - Новый пароль должен отличаться от старого

4. **Загрузка файлов:**
   - Проверка типа файла (только изображения)
   - Ограничение размера (5MB)
   - Автоматическая оптимизация и ресайз через Sharp
   - Уникальные имена файлов
   - Локальное хранение в `/public/uploads/`

## Использование

### Редактирование профиля:

1. Откройте страницу `/profile`
2. Нажмите кнопку "Редактировать" в секции "Основная информация"
3. Измените нужные поля
4. Нажмите "Сохранить изменения"

### Изменение аватара:

1. Нажмите "Изменить фото" под аватаром
2. Выберите изображение
3. Дождитесь загрузки

### Изменение пароля:

1. Нажмите "Изменить пароль" в секции "Безопасность"
2. Введите текущий пароль
3. Введите новый пароль дважды
4. Нажмите "Изменить пароль"

## Валидация полей

### Имя и Фамилия:

- Обязательны для заполнения
- Не могут быть пустыми или состоять только из пробелов

### Телефон:

- Обязателен для заполнения
- Формат: от 10 до 15 цифр
- Может содержать + в начале
- Пробелы, дефисы и скобки игнорируются

### Telegram:

- Необязательное поле
- Обычно начинается с @

### WhatsApp:

- Необязательное поле
- Формат номера телефона

### Пароль:

- Минимум 6 символов
- Новый пароль должен отличаться от текущего
- Требуется подтверждение

## Toast-уведомления

Все операции сопровождаются уведомлениями:

- ✅ Успешное сохранение
- ❌ Ошибки валидации
- ⚠️ Предупреждения
- ℹ️ Информационные сообщения

## Адаптивный дизайн

Страница профиля полностью адаптивна:

- **Desktop (>1200px):** Двухколоночный макет
- **Tablet (768-1199px):** Оптимизированный макет
- **Mobile (<768px):** Одноколоночный макет, стекированные элементы

## Производительность

### Оптимизации:

- Server-Side Rendering (SSR) для первичной загрузки
- Automatic Static Optimization где возможно
- Revalidation кэша после изменений
- Оптимизированные изображения через Sharp (локальное хранение)
- Ленивая загрузка модальных окон

## Troubleshooting

### Не сохраняются изменения профиля

1. Проверьте консоль браузера на ошибки
2. Убедитесь, что вы авторизованы
3. Проверьте правильность заполнения обязательных полей
4. Попробуйте выйти и войти заново

### Не удается изменить пароль

1. Убедитесь, что правильно ввели текущий пароль
2. Проверьте, что новый пароль минимум 6 символов
3. Убедитесь, что пароли в полях "Новый пароль" и "Подтверждение" совпадают

### Аватар не загружается

1. Проверьте, что папка `/public/uploads/avatars/` существует
2. Убедитесь, что размер файла не превышает 5MB
3. Проверьте формат файла (должно быть изображение)
4. См. `AVATAR_UPLOAD_TEST.md` для дополнительной информации

## Будущие улучшения

Возможные расширения функционала:

- [ ] История изменений профиля
- [ ] Двухфакторная аутентификация (2FA)
- [ ] Загрузка нескольких фото (галерея)
- [ ] Смена email с подтверждением
- [ ] Экспорт данных профиля
- [ ] Удаление аккаунта
- [ ] Настройки приватности
- [ ] Уведомления (email/push)
