# 🚀 Быстрый старт: Импорт прайса из CSV

## Установка

```bash
npm install csv-parse
```

✅ **Уже установлено** в вашем проекте!

---

## 📂 Подготовка данных

### 1. Создайте директорию для CSV

```bash
mkdir data
```

### 2. Экспортируйте листы Excel в CSV

Из файла `Прайс РРЦ.xlsx` экспортируйте листы:

- `PRICE_INVERTERS` → `./data/PRICE_INVERTERS.csv`
- `PRICE_MODULES` → `./data/PRICE_MODULES.csv`
- `PRICE_ESS` → `./data/PRICE_ESS.csv`
- `PRICE_BATT` → `./data/PRICE_BATT.csv`
- `PRICE_MOUNT_BOS` → `./data/PRICE_MOUNT_BOS.csv`

**⚠️ ВАЖНО:** Заголовки колонок в CSV должны **ТОЧНО** совпадать с Excel!

---

## ▶️ Запуск импорта

### Готовые скрипты

```bash
# Инверторы
node scripts/importInvertersFromCsv.js

# Солнечные панели
node scripts/importModulesFromCsv.js
```

### Создание скрипта для новой категории

#### Вариант 1: Скопировать существующий

```bash
cp scripts/importInvertersFromCsv.js scripts/importEssFromCsv.js
```

Затем измените:

- путь к CSV: `./data/PRICE_ESS.csv`
- код категории: `"ess"`
- функцию маппинга (скопируйте из `scripts/importEss.js`)

#### Вариант 2: Использовать шаблон

```bash
cp scripts/importFromCsv.TEMPLATE.js scripts/importEssFromCsv.js
```

Следуйте инструкциям в комментариях шаблона.

---

## 📊 Что делает скрипт

✅ Читает CSV построчно (эффективно для больших файлов)  
✅ Проверяет наличие категории в БД  
✅ **Автоматически обновляет** дубликаты по `sku` (UPSERT)  
✅ Пропускает битые строки (без SKU/Наименования)  
✅ Логирует ошибки, но продолжает работу  
✅ Сохраняет структуру `attrs` из Excel-импорта

**Вывод:**

```
🎉 Импорт Инверторы завершён.
➕ Добавлено новых: 42
🔄 Обновлено: 13
⚠️ Пропущено: 3
```

---

## 🔧 Структура файлов

```
scripts/
├── csvImportHelpers.js           # Общие функции (toNum, toBool, upsert и т.п.)
├── importInvertersFromCsv.js     # Готовый скрипт для инверторов
├── importModulesFromCsv.js       # Готовый скрипт для панелей
├── importFromCsv.TEMPLATE.js     # Шаблон для новых категорий
└── CSV_IMPORT_README.md          # Полная документация
```

---

## ⚠️ Важные правила

### ❌ НЕ МЕНЯТЬ:

- Схему БД (`price_items`)
- Структуру `attrs` (верхний уровень: `electrical`, `mechanical`, `compat`, `bos`, `meta`)
- Названия ключей внутри `attrs` (они должны совпадать с Excel-импортом)
- Логику парсеров (`toNum`, `toBool`, `parseGridType` и т.п.)

### ✅ МОЖНО МЕНЯТЬ:

- Путь к CSV-файлу
- Код категории (`categoryCode`)
- Тип записи (`typeCode`)
- Какие колонки CSV читаются (`row["Название_колонки"]`)

---

## 🐛 Частые проблемы

### Ошибка: "Категория 'XXX' не найдена"

**Решение:** Добавьте категорию в БД:

```sql
INSERT INTO price_categories (code, title, group_code)
VALUES ('ess', 'Системы накопления энергии', 'core');
```

### Ошибка: "Cannot find module 'csv-parse'"

**Решение:**

```bash
npm install csv-parse
```

### Ошибка: колонка не найдена в CSV

**Решение:** Проверьте, что имена колонок **ТОЧНО** совпадают:

```javascript
// В коде:
row["Мощность_кВт"]

// В CSV:
Мощность_кВт  ✅
мощность_квт  ❌
Мощность кВт  ❌
```

---

## 📚 Полная документация

Смотрите: **[scripts/CSV_IMPORT_README.md](scripts/CSV_IMPORT_README.md)**

---

## ✨ Преимущества CSV-импорта

✅ Быстрее Excel (stream-обработка)  
✅ Меньше зависимостей (не нужен полный файл в памяти)  
✅ Удобно для CI/CD (автоматизация обновления прайса)  
✅ Переиспользует логику Excel-импорта (консистентность данных)  
✅ Автоматическое обновление при изменении цен (UPSERT)

---

**Вопросы?** Проверьте примеры: `scripts/importInvertersFromCsv.js`, `scripts/importModulesFromCsv.js`

---

## 📚 Полная документация

Смотрите **[scripts/README_CSV_IMPORT.md](scripts/README_CSV_IMPORT.md)** — полное оглавление всех материалов.
