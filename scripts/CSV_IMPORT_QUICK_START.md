# 🚀 Quick Start: Импорт прайса из CSV

## ⚡ Быстрый старт за 2 минуты

### Шаг 1: Подготовь CSV-файл

Экспортируй нужный лист из Excel в CSV и положи в `./data/`:

```
./data/
  ├── PRICE_INVERTERS.csv
  ├── PRICE_MODULES.csv
  ├── PRICE_ESS.csv
  ├── PRICE_BATT.csv
  └── ... другие категории
```

**⚠️ Важно:** заголовки колонок должны совпадать с Excel!

---

### Шаг 2: Запусти импорт

```bash
# Инверторы
node scripts/importInvertersFromCsv.js

# Солнечные модули
node scripts/importModulesFromCsv.js

# ESS (системы накопления)
node scripts/importEssFromCsv.js

# Батареи
node scripts/importBattFromCsv.js

# Крепёж и BOS
node scripts/importMountBosFromCsv.js
```

**Полный список команд:** `scripts/CSV_IMPORT_COMMANDS.md`

---

### Шаг 3: Проверь результат

```bash
# Проверь количество записей
node scripts/check-categories.js
```

---

## 📋 Что происходит при импорте?

1. **Читается CSV** построчно с автоопределением кодировки
2. **Парсятся данные** через те же функции, что и в Excel-импорте
3. **UPSERT в БД:**
   - Если `SKU` не существует → **вставка** новой записи
   - Если `SKU` существует → **обновление** цены, наличия, приоритета и `attrs`
4. **Выводится статистика:** добавлено, обновлено, пропущено

---

## 📊 Пример вывода

```
============================================================
🚀 Импорт: Инверторы
============================================================

⠋ Обработано строк: 127 | Время: 8s

============================================================
✅ Импорт завершён: Инверторы
============================================================

📊 Статистика:
   ➕ Добавлено новых записей: 45
   🔄 Обновлено существующих: 78
   ⚠️  Пропущено (ошибки/пустые): 4
   📝 Всего обработано строк: 127

============================================================
```

---

## 🔄 UPSERT — обновление существующих записей

При импорте используется `INSERT ... ON DUPLICATE KEY UPDATE`, поэтому:

✅ **Можно переимпортировать** один и тот же CSV сколько угодно раз  
✅ **Обновляются** цены, наличие, приоритет, attrs  
✅ **Не падает** на дубликатах SKU  
✅ **Безопасно** для продакшена

---

## 📝 Структура данных (НЕ МЕНЯЕТСЯ)

Все CSV-импортеры используют **ту же структуру `attrs`**, что и Excel-импортеры:

```typescript
type PriceItemAttrs = {
  electrical?: {
    // Электрические параметры: мощность, напряжение, КПД, MPPT и т.п.
  };
  mechanical?: {
    // Габариты, вес, материал, нагрузки
  };
  compat?: {
    // Совместимость: тип кровли, сегменты B2C/B2B, LV/HV и т.п.
  };
  bos?: {
    // Обвязка и работы: кабели, стоимость работ, деградация
  };
  meta?: {
    // Вспомогательные данные: бренд, гарантия, сырые значения
  };
};
```

**⚠️ КРИТИЧЕСКИ ВАЖНО:** не добавляй новые разделы верхнего уровня!

---

## 🛠️ Создание импортера для новой категории

### Вариант 1: Быстрое копирование (рекомендуется)

```bash
# 1. Скопируй шаблон
cp scripts/importFromCsv.TEMPLATE.js scripts/importCpo90FromCsv.js

# 2. Открой файл и измени 4 строки:
#    CSV_PATH = "./data/PRICE_CPO90.csv"
#    CATEGORY_CODE = "cpo90"
#    TYPE_CODE = "cpo90"
#    CATEGORY_NAME = "CPO90"

# 3. Скопируй функцию маппинга из scripts/importCpo90.js

# 4. Запусти
node scripts/importCpo90FromCsv.js
```

### Вариант 2: С подробными комментариями

```bash
# Используй улучшенный шаблон с примерами
cp scripts/importFromCsv.TEMPLATE_v2.js scripts/importXxxFromCsv.js
```

---

## 📚 Полная документация

- **Подробное руководство:** `scripts/CSV_IMPORT_GUIDE.md`
- **Справочник категорий:** `scripts/CSV_CATEGORIES_REFERENCE.md`
- **Все команды:** `scripts/CSV_IMPORT_COMMANDS.md`
- **Основной README:** `scripts/CSV_IMPORT_README.md`

---

## ✅ Чеклист перед первым импортом

- [ ] CSV-файл лежит в `./data/`
- [ ] Заголовки колонок совпадают с Excel
- [ ] Категория существует в БД (проверь: `node scripts/check-categories.js`)
- [ ] Сделан backup БД (на всякий случай)
- [ ] Тестовый запуск на 5-10 строках прошёл успешно

---

## 🐛 Проблемы и решения

### Ошибка: "Категория не найдена"

```bash
# Проверь, есть ли категория в БД
node scripts/check-categories.js

# Если нет — добавь через:
node scripts/update-categories.js
```

### Ошибка: "Duplicate entry for key 'sku'"

Это нормально! UPSERT автоматически обновит существующую запись.

### Кодировка сломалась (кракозябры)

CSV должен быть в **UTF-8 с BOM**. В Excel при экспорте выбери:
- "CSV UTF-8 (разделитель - запятая)"

### Нет некоторых колонок в CSV

Проверь, что заголовки **точно совпадают** с Excel (включая пробелы, подчёркивания).

---

## 💡 Советы и best practices

### 1. Тестируй на малой выборке

Перед массовым импортом создай тестовый CSV с 5-10 строками:

```bash
head -n 11 ./data/PRICE_INVERTERS.csv > ./data/PRICE_INVERTERS_TEST.csv
```

Запусти импорт на тесте и проверь корректность данных в БД.

### 2. Версионный контроль CSV

Храни CSV в git, чтобы отслеживать изменения прайса:

```bash
git add data/*.csv
git commit -m "Обновление прайса от 2025-11-19"
```

### 3. Автоматизируй обновления

Настрой cron для периодического импорта актуальных цен:

```bash
# Каждый день в 3:00
0 3 * * * cd /path/to/project && node scripts/importInvertersFromCsv.js
```

### 4. Логируй результаты

Перенаправь вывод в файл для истории импортов:

```bash
node scripts/importInvertersFromCsv.js >> logs/import_$(date +\%Y\%m\%d).log 2>&1
```

---

## 🔗 Готовые CSV-импортеры (полный список)

| Категория | Команда |
|-----------|---------|
| Инверторы | `node scripts/importInvertersFromCsv.js` |
| Модули | `node scripts/importModulesFromCsv.js` |
| ESS | `node scripts/importEssFromCsv.js` |
| Батареи | `node scripts/importBattFromCsv.js` |
| Крепёж/BOS | `node scripts/importMountBosFromCsv.js` |
| Кабели | `node scripts/importCableFromCsv.js` |
| Коннекторы | `node scripts/importConnectorFromCsv.js` |
| Предохранители | `node scripts/importFuseFromCsv.js` |
| Лотки | `node scripts/importLotkiFromCsv.js` |
| Эл. панели | `node scripts/importElPanelFromCsv.js` |
| Трансформаторы | `node scripts/importTransFromCsv.js` |
| УЗИПы | `node scripts/importUzipFromCsv.js` |
| Счётчики | `node scripts/importSmartmetersFromCsv.js` |
| CPO90 | `node scripts/importCpo90FromCsv.js` |
| PowOff | `node scripts/importPowOffFromCsv.js` |

---

## 🎯 Пример: полный цикл обновления прайса

```bash
# 1. Экспортируй листы из Excel в CSV (в папку ./data/)

# 2. Импортируй основное оборудование
node scripts/importInvertersFromCsv.js
node scripts/importModulesFromCsv.js
node scripts/importEssFromCsv.js
node scripts/importBattFromCsv.js

# 3. Импортируй BOS-компоненты
node scripts/importMountBosFromCsv.js
node scripts/importCableFromCsv.js
node scripts/importConnectorFromCsv.js

# 4. Проверь результат
node scripts/check-categories.js

# 5. Проверь сироты (SKU без категории)
node scripts/check-orphan-items.js
```

---

**🎉 Готово! Теперь ты можешь импортировать любой прайс из CSV за пару минут.**

**Есть вопросы?** Читай подробное руководство: `scripts/CSV_IMPORT_GUIDE.md`



