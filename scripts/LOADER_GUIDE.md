# 🎬 Loader и прогресс-бар для CSV-импорта

## ✨ Что добавлено

Во время импорта CSV теперь отображается **анимированный loader** с:

- ⏱️ Счётчиком обработанных строк
- ⏰ Таймером времени выполнения
- 🎨 Визуальной анимацией spinner
- 📊 Красивым итоговым отчётом
- ❌ Списком ошибок (если есть)

---

## 🎥 Как это выглядит

### Стандартный режим (без verbose)

```
============================================================
🚀 Импорт: Инверторы
============================================================

⠋ Обработано строк: 247 | Время: 12s

============================================================
✅ Импорт завершён: Инверторы
============================================================

📊 Статистика:
   ➕ Добавлено новых записей: 42
   🔄 Обновлено существующих: 13
   ⚠️  Пропущено (ошибки/пустые): 2
   📝 Всего обработано строк: 247

❌ Ошибки (2):
   • INV-999: Отсутствует обязательное поле Мощность_кВт
   • INV-998: Некорректный формат цены

============================================================
```

### Verbose режим (детальные логи)

```
============================================================
🚀 Импорт: Инверторы
============================================================

⠋ Обработано строк: 15 | Время: 3s
➕ Добавлено: Huawei SUN2000-5KTL-L1 (INV-001)
⠙ Обработано строк: 16 | Время: 3s
🔄 Обновлено: Growatt MIN 6000TL-XH (INV-002)
⠹ Обработано строк: 17 | Время: 4s
❌ Ошибка: INV-999 - Отсутствует обязательное поле Мощность_кВт
⠸ Обработано строк: 18 | Время: 4s
```

---

## 🔧 Использование

### Стандартный режим (рекомендуется)

По умолчанию loader включен автоматически:

```javascript
await importFromCsv({
  csvPath: "./data/PRICE_INVERTERS.csv",
  categoryCode: "inverter",
  categoryName: "Инверторы",
  mapRowToPriceItem: mapInverterRowToPriceItem,
  db,
  schema: { priceCategories, priceItems },
  // verbose не указан → используется false (стандартный режим)
});
```

### Verbose режим (для отладки)

Включите детальные логи:

```javascript
await importFromCsv({
  csvPath: "./data/PRICE_INVERTERS.csv",
  categoryCode: "inverter",
  categoryName: "Инверторы",
  mapRowToPriceItem: mapInverterRowToPriceItem,
  db,
  schema: { priceCategories, priceItems },
  verbose: true, // 🔹 Показывать каждую обработанную запись
});
```

---

## 📊 Элементы loader

### Анимированный spinner

```
⠋ ⠙ ⠹ ⠸ ⠼ ⠴ ⠦ ⠧ ⠇ ⠏  (циклично)
```

Обновляется каждые 80ms для плавной анимации.

### Счётчик строк

```
Обработано строк: 247
```

Обновляется в реальном времени при обработке каждой строки CSV.

### Таймер

```
Время: 12s
```

Показывает время с момента запуска импорта.

### Итоговая статистика

```
📊 Статистика:
   ➕ Добавлено новых записей: 42
   🔄 Обновлено существующих: 13
   ⚠️  Пропущено (ошибки/пустые): 2
   📝 Всего обработано строк: 247
```

### Список ошибок

**Если ошибок ≤ 10:**

```
❌ Ошибки (2):
   • INV-999: Отсутствует обязательное поле Мощность_кВт
   • INV-998: Некорректный формат цены
```

**Если ошибок > 10:**

```
❌ Ошибок: 25 (показаны первые 10, остальные пропущены)
   • INV-999: Отсутствует обязательное поле Мощность_кВт
   • INV-998: Некорректный формат цены
   • INV-997: ...
   (ещё 7 ошибок)
```

---

## 🎯 Когда использовать verbose режим

### ✅ Используйте verbose для:

- 🐛 Отладки скрипта импорта
- 🔍 Поиска проблемных записей
- 📝 Проверки корректности маппинга
- 🧪 Тестового импорта (5-10 строк)

### ❌ НЕ используйте verbose для:

- 🚀 Боевого импорта больших файлов (>100 строк)
- ⚡ Массового импорта нескольких категорий
- 📊 Регулярного обновления прайса

**Причина:** verbose режим замедляет импорт (каждый `console.log` — это I/O операция).

---

## 💡 Технические детали

### Реализация

```javascript
class ImportLoader {
  constructor(categoryName) {
    this.frames = ["⠋", "⠙", "⠹", "⠸", "⠼", "⠴", "⠦", "⠧", "⠇", "⠏"];
    this.currentFrame = 0;
    this.interval = null;
    this.processed = 0;
    this.lastUpdate = Date.now();
  }

  start() {
    console.clear(); // Очистка консоли
    // Отображение заголовка
    this.interval = setInterval(() => {
      // Анимация spinner + счётчик + таймер
    }, 80);
  }

  update(count) {
    this.processed = count; // Обновление счётчика
  }

  stop() {
    clearInterval(this.interval); // Остановка анимации
    // Очистка строки spinner
  }
}
```

### Особенности

- **Stream-обработка**: loader не блокирует чтение CSV
- **Minimal overhead**: обновление каждые 80ms (не каждую строку)
- **Graceful shutdown**: корректная остановка при ошибках
- **Cross-platform**: работает в Windows/Linux/Mac

---

## 🔄 Возврат данных

Функция `importFromCsv` возвращает объект:

```javascript
{
  inserted: 42,   // количество добавленных записей
  updated: 13,    // количество обновлённых записей
  skipped: 2,     // количество пропущенных записей
  errors: [       // массив ошибок
    { sku: "INV-999", error: "Отсутствует поле..." },
    { sku: "INV-998", error: "Некорректный формат..." }
  ]
}
```

Можно использовать для дальнейшей обработки:

```javascript
const result = await importFromCsv({ ... });

if (result.errors.length > 0) {
  // Сохранить ошибки в файл
  fs.writeFileSync(
    "import-errors.json",
    JSON.stringify(result.errors, null, 2)
  );
}
```

---

## 📚 Примеры

### Пример 1: Стандартный импорт

```javascript
// scripts/importInvertersFromCsv.js
async function importInvertersFromCsv() {
  try {
    const csvPath = resolve(process.cwd(), "./data/PRICE_INVERTERS.csv");

    const result = await importFromCsv({
      csvPath,
      categoryCode: "inverter",
      categoryName: "Инверторы",
      mapRowToPriceItem: mapInverterRowToPriceItem,
      db,
      schema: { priceCategories, priceItems },
    });

    process.exit(0);
  } catch (err) {
    console.error("❌ Критическая ошибка:", err);
    process.exit(1);
  }
}
```

### Пример 2: Verbose режим для отладки

```javascript
async function importInvertersFromCsv() {
  try {
    const csvPath = resolve(process.cwd(), "./data/test_inverters.csv");

    const result = await importFromCsv({
      csvPath,
      categoryCode: "inverter",
      categoryName: "Инверторы (ТЕСТ)",
      mapRowToPriceItem: mapInverterRowToPriceItem,
      db,
      schema: { priceCategories, priceItems },
      verbose: true, // 🔹 Детальные логи
    });

    console.log("\n📋 Детальный отчёт:");
    console.log(`Добавлено: ${result.inserted}`);
    console.log(`Обновлено: ${result.updated}`);
    console.log(`Пропущено: ${result.skipped}`);

    if (result.errors.length > 0) {
      console.log("\n❌ Список ошибок:");
      result.errors.forEach(({ sku, error }) => {
        console.log(`  ${sku}: ${error}`);
      });
    }

    process.exit(0);
  } catch (err) {
    console.error("❌ Критическая ошибка:", err);
    process.exit(1);
  }
}
```

### Пример 3: Сохранение отчёта в файл

```javascript
import { writeFileSync } from "fs";

async function importWithReport() {
  const result = await importFromCsv({ ... });

  const report = {
    timestamp: new Date().toISOString(),
    category: "inverter",
    stats: {
      inserted: result.inserted,
      updated: result.updated,
      skipped: result.skipped,
    },
    errors: result.errors,
  };

  writeFileSync(
    `import-report-${Date.now()}.json`,
    JSON.stringify(report, null, 2)
  );

  console.log("\n💾 Отчёт сохранён в файл");
}
```

---

## 🎓 Итоги

✅ **Loader работает автоматически** — не требует настройки  
✅ **Красивый вывод** — приятно смотреть на импорт  
✅ **Не замедляет работу** — обновление каждые 80ms, а не каждую строку  
✅ **Подробная статистика** — всё, что нужно знать об импорте  
✅ **Сбор ошибок** — все проблемы в одном месте

**Документация:**

- 📖 [Полная документация](CSV_IMPORT_README.md)
- 🚀 [Быстрый старт](../CSV_IMPORT_QUICKSTART.md)
- 📋 [Команды](CSV_IMPORT_COMMANDS.md)
